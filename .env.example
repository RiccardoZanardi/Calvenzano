# Configurazione autenticazione
PASSWORD_HASH=your_bcrypt_password_hash_here
SESSION_SECRET=your_session_secret_here

# Configurazione ambiente
NODE_ENV=development
PORT=3002

# Configurazione Session Store (OPZIONALE)
# Se configurato, userà MongoDB per le sessioni in produzione (più stabile su Render)
# Altrimenti userà MemoryStore (adatto per sviluppo locale)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database

# Configurazione GitHub Storage (per persistenza dati su Render)
# Queste variabili sono OPZIONALI - se non configurate, l'app userà solo file locali
# Per abilitare la persistenza su Render, crea un repository GitHub e configura:

# Il tuo username GitHub
GITHUB_OWNER=your_github_username

# Nome del repository dove salvare i dati (può essere privato)
GITHUB_REPO=multe-data

# Personal Access Token con permessi 'repo' (per repository privati) o 'public_repo' (per repository pubblici)
# Crea il token su: https://github.com/settings/tokens
GITHUB_TOKEN=ghp_your_personal_access_token_here

# Branch del repository (opzionale, default: main)
GITHUB_BRANCH=main

# ISTRUZIONI PER CONFIGURARE GITHUB STORAGE:
# 1. Crea un repository GitHub (può essere privato)
# 2. Genera un Personal Access Token con permessi 'repo'
# 3. Configura le variabili d'ambiente sopra
# 4. L'app salverà automaticamente i dati nel repository
# 5. Su Render, aggiungi queste variabili nelle Environment Variables